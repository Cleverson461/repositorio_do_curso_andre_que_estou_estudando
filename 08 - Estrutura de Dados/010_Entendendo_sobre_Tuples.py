
# Tuples
    # Armazenar mais de uma informação em variáveis
    # Manter a sequencia dos dados em uma variável
    # Não podem ser alteradas (Immutable)
    # Tuples são imutáveis (não podem ser alteradas após criação)
    # Permite adicionar, remover e alterar dados
    # Indexação: Utilizar números para acessar os dados
    # Tuples podem conter diferentes tipos de dados (heterogêneos)
    # Tuples em Python são implementados como tuplas
    # Tuples podem ser criadas usando parênteses ()
    # Tuples podem ser vazios ()
    # Tuples podem conter outros tuples como elementos
    # Tuples podem ser concatenados com o operador +
    # Tuples podem ser repetidas com o operador *
    # Tuples podem ser ordenados com a função sorted()
    # Tuples podem ser invertidas com a função reversed()
    # Tuples podem ser buscadas com a função index()
    # Tuples podem ser filtradas com a função filter()
    
cores_list = ['amarelo', 'verde', 'azul', 'vermelho']
cores_tuple = ('amarelo', 'verde', 'azul', 'vermelho')


#cores_tuple.append('Roxo')
#print(cores_list * 1000)
print(cores_tuple * 1000)